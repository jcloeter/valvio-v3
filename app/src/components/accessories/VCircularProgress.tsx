import React from 'react';
import CircularProgress from "@mui/material/CircularProgress";

const VCircularProgress = () => {
    return (
        <div>
            <CircularProgress color = "primary"/>
        </div>
    );
};

export default VCircularProgress;